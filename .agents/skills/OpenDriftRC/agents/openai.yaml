interface:
  display_name: "OpenDriftRC"
  short_description: "Repo-specific patterns and workflows for OpenDriftRC"
  default_prompt: "Use the OpenDriftRC repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true